<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logotkpay"
        android:label="TKPAY"
        android:roundIcon="@drawable/logotkpay"
        android:supportsRtl="true"
        android:theme="@style/Theme.KotlinProject"
        tools:targetApi="31">
        <activity
            android:name=".SharedWalletDetail"
            android:exported="false"
            android:label="@string/title_activity_shared_wallet_detail"
            android:theme="@style/Theme.KotlinProject" />
        <activity
            android:name=".AddSharedWallet"
            android:exported="false"
            android:label="@string/title_activity_add_shared_wallet"
            android:theme="@style/Theme.KotlinProject" />
        <activity
            android:name=".SharedWallet"
            android:exported="false" />

        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="false" />
    </application>

</manifest>